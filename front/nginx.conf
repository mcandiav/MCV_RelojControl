server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Tras cada deploy, el index apunta a nuevos hashes; no cachear el HTML.
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
    }

    # URL del API en runtime; debe poder cambiarse sin rebuild del JS.
    location = /api-config.js {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri =404;
    }

    # Assets con hash: si no existen, 404 (nunca devolver index.html como si fuera JS/CSS).
    location /js/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }

    location /css/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
